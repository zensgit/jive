# Copy to /tmp/jive-saas-staging.env and fill with real staging values.
# Do not commit the filled file.
#
# Hosted Supabase Edge Functions already provide:
#   SUPABASE_URL
#   SUPABASE_ANON_KEY
#   SUPABASE_SERVICE_ROLE_KEY
#
# Keep them here only if you also want to reuse this file for local functions serve
# or for client APK builds.
#
# Core SaaS rollout needs only:
#   ADMIN_API_TOKEN
#   ADMIN_API_ALLOWED_ORIGINS
#   ANALYTICS_ADMIN_TOKEN
#   NOTIFICATION_ADMIN_TOKEN
#
# Billing rollout additionally needs the Google / Apple / webhook variables below.

# Optional for hosted deploy. Useful for local serve and client APK builds.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional until billing rollout.
GOOGLE_SERVICE_ACCOUNT_EMAIL=
# Keep embedded newlines escaped as \n on a single line.
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=
GOOGLE_PLAY_PACKAGE_NAME=

# Optional until billing rollout.
APPLE_APP_STORE_BUNDLE_ID=
APPLE_APP_STORE_SHARED_SECRET=
APPLE_APP_STORE_APPLE_ID=
APPLE_APP_STORE_ENVIRONMENT=

# Optional until billing rollout.
PUBSUB_BEARER_TOKEN=
WEBHOOK_HMAC_SECRET=
DOMESTIC_PAYMENT_WEBHOOK_TOKEN=
# Optional self-hosted base URL for mock domestic payment checkout pages.
DOMESTIC_PAYMENT_MOCK_BASE_URL=

# Required for core SaaS rollout.
ADMIN_API_TOKEN=
ADMIN_API_ALLOWED_ORIGINS=
ANALYTICS_ADMIN_TOKEN=
NOTIFICATION_ADMIN_TOKEN=
